<!DOCTYPE html>
<html lang="en">
    <%- include ('./partials/head.ejs', {page: 'Recipe Blog'}) %>

    <%- include ('./partials/nav.ejs') %>

<body>

<h1 class="title"><%= foundRecipe.title %></h1>
<img class ="image" src="<%= foundRecipe.image %>"/>
<div class="container">
    <div class="recipe" id="ingredients">
       <h3 class="title">Ingredients:</h3>
       
    </div>
    <div class="recipe" id="prep">
        <h3 class="title">Preperation:</h3>
        <% for(let idx = 0; idx < foundRecipe.recipe.length; idx++){ %>
        <p><%= foundRecipe.recipe[idx]%></p>
        <%}%>
    </div>  
</div>
<div class="reviewContainer">
    <div class="reviews"> 
        ~~~~some reviews~~~~ 
    </div>
</div>

</body>
</html>